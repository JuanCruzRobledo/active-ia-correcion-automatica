## 0. Red de seguridad

- [x] 0.1 Capturar baseline: `cd backend && pytest -q` (esperado: 1610 passed, 1 failed pre-existente de passlib/bcrypt) y `cd frontend && npm run typecheck && npm run lint && npx vitest run`. Anotar los números; cualquier rojo nuevo posterior es regresión, no "ajuste". **Resultado**: backend 1610 passed, 1 failed (`test_pendientes_moodle_returns_424_when_no_credentials`, exactamente el esperado). Frontend: no existía script `typecheck` — se agregó (`tsc -b`, `noEmit` ya en tsconfig) — 0 errores; `lint` 70 problemas preexistentes (66 errores, 4 warnings, ninguno tocado por este change); `vitest run` 15 archivos / 73 tests, todos pasando
- [x] 0.2 Verificar contra el backend levantado que `POST /auth/switch-universidad` acepta `universidad_id` nulo para un superadmin (D6). Si NO lo acepta, agregar la extensión del endpoint como tarea 2.x antes de seguir. **Resultado**: verificado por código (no por servidor levantado, ver 5.2) — `SeleccionarUniversidadRequest.universidad_id: int` es obligatorio, NO acepta `null`. Gap real. Se agregó la tarea **2.0** para extenderlo antes de seguir con el resto del Grupo 2
- [x] 0.3 Confirmar que `GET /perfil` monta `get_universidad_activa` y por lo tanto NO sirve para poblar el selector (D5), dejando registro del motivo. **Resultado**: confirmado, `app/routers/perfil.py:91` monta `Depends(get_universidad_activa)`. Motivo por el que no sirve: (a) un no-superadmin con 2+ membresías y sin `universidad_activa_id` en el token recibe 409 en vez de una lista — exactamente el deadlock que describe D5; (b) aunque no diera 409, el endpoint sólo devuelve datos de la universidad activa (moodle_username/host de UNA membresía), nunca una lista de universidades

## 1. Roturas de integración (frontend)

- [x] 1.1 `shared/types/index.ts`: `UserInfo` suma `universidad_activa_id: number | null` y `es_superadmin: boolean`; `rol` pasa a `Rol | null`. Agregar los tipos `UniversidadDisponible`, `SeleccionUniversidadRequerida` y la unión `LoginResult`
- [x] 1.2 Correr `npm run typecheck` y anotar la lista completa de errores que produce 1.1: es el inventario real de lugares a tocar. **Resultado**: 1 error inicial en `hasAnyRole` (`user.rol` pasado a `roles.includes()` sin narrowing) — corregido con `user?.rol ? ... : false`. Después de eso, **0 errores downstream**: el resto del código ya leía `.rol` de forma defensiva (`user?.rol || 'admin'`), así que el compilador no marcó ningún otro sitio. El inventario real de "lugares a tocar" se obtuvo por grep de `\.rol\b`: 16 archivos (coincide con el design), quedan documentados para la migración a `useTenant()` en 3.6
- [x] 1.3 `auth-service.ts`: `login()` discrimina por `requiere_seleccion` y sólo persiste sesión cuando hay `access_token`. Nunca escribir el literal `"undefined"` en `auth_token`. Agregar `seleccionarUniversidad(tokenTransicion, universidadId)`
- [x] 1.4 `useLogin.ts`: dejar de asumir `data.user`; en la rama de selección no navegar ni mostrar el toast de bienvenida
- [x] 1.5 `LoginPage.tsx`: segundo paso con la lista de universidades (nombre + rol) y opción de volver al formulario descartando el `token_transicion`. Se agregó `useSeleccionarUniversidad` (mismo comportamiento de éxito que `useLogin`: toast + navegación por `primer_login`)
- [x] 1.6 Surfacear el 403 de "usuario sin universidad asignada" como mensaje accionable, distinguible de credenciales inválidas. Se extrae el detail real del backend (`mensajeDeError`) en vez del `error.message` genérico de axios, tanto en el form como en la pantalla de selección (401 de token vencido)
- [x] 1.7 `navConfig.ts`: extraer la regla inline a `puedeVer(item, { rol, esSuperadmin })` con bypass de superadmin (D3); actualizar `Sidebar.tsx` y `BottomNav.tsx` para usarla. Nota: usan `user.es_superadmin` de `useAuth()` como fuente interina — la migración a `useTenant()` es la tarea 3.6
- [x] 1.8 `PerfilPage.tsx` + `perfil/types`: sacar `moodle_host` del schema Zod y del payload del PATCH; mostrarlo read-only desde la universidad activa, con aviso explícito cuando está vacío. Nota: `PerfilPage.tsx` ya excedía las 200 LOC del proyecto ANTES de este change (845 líneas) — no introducido por este change, señalado en el resumen final
- [x] 1.9 `api-client.ts`: en `handleResponseError`, distinguir el 409 de "sin universidad activa" del 409 genérico, y agregar el caso 424 con mensaje que remita al perfil. El backend no manda `error_code` para el 409 de `get_universidad_activa` (sólo el string de detail) — se distingue por contenido del mensaje (`/eleg[ií].*universidad/i`), documentado como fragilidad conocida en el código
- [x] 1.10 Tests: `auth-service.login()` en sus dos ramas (con y sin `access_token`), incluyendo que la rama de selección NO escribe en `localStorage` — `src/features/auth/services/auth-service.test.ts`
- [x] 1.11 Tests: `puedeVer` — superadmin con `rol = null` ve todo; no-superadmin con `rol = null` ve sólo los items sin `roles` — `src/shared/components/layout/navConfig.test.ts`
- [x] 1.12 Tests: `handleResponseError` para 409-sin-universidad y 424 — `src/shared/services/api-client.test.ts`
- [x] 1.13 `npm run typecheck` y `npm run lint` limpios; `npx vitest run` sin regresiones respecto de 0.1. **Resultado**: typecheck 0 errores; lint 70 problemas (idéntico al baseline, ninguno nuevo); vitest 20 archivos / 101 tests pasando (baseline: 15/73 — se sumaron 28 tests nuevos, 0 regresiones)

## 2. Endpoints de universidades (backend)

- [x] 2.0 **Gap detectado en 0.2**: `SeleccionarUniversidadRequest.universidad_id` es `int` obligatorio — `POST /auth/switch-universidad` NO acepta `universidad_id` nulo para el modo global del superadmin (D6). Extender: `universidad_id: int | None`, y en `AuthService._resolver_y_emitir`/`switch_universidad` manejar el caso `None` — sólo válido para `es_superadmin`, emite token con `universidad_id=None`, `rol=None`; un no-superadmin que envía `null` recibe 403 ("no podés operar en modo global sin ser superadmin"). `seleccionar_universidad` (paso 2 del login) comparte el mismo schema pero no se usa con `null` en la práctica — se cubre igual por completitud del tipo
- [x] 2.1 `app/schemas/universidad.py`: `UniversidadResponse`, `UniversidadListItem`, `UniversidadList`, `UniversidadCreate`, `UniversidadUpdate` y `UniversidadPropia` (con el rol del usuario)
- [x] 2.2 `universidad_repository.py`: listado paginado con `include_inactive`, búsqueda por nombre para el chequeo de duplicado, y listado de universidades del usuario vía `UsuarioUniversidad` activa
- [x] 2.3 `app/services/universidad_service.py`: `listar_mias` (superadmin ⇒ todas las activas con rol ADMIN sintético), `listar`, `crear` (409 si el nombre existe), `actualizar` (404 si no existe) y `eliminar` como baja lógica
- [x] 2.4 `app/routers/universidades.py`: `GET /universidades/mias` **sólo con autenticación, sin `get_universidad_activa`** (D5), y el resto del ABM con guard de `ctx.es_superadmin`
- [x] 2.5 Montar el router en `app/main.py`
- [x] 2.6 Tests: `GET /universidades/mias` — una membresía, dos membresías con roles distintos, membresía inactiva excluida, superadmin ve todas, superadmin en modo global responde 200 (no 409), sin token responde 401 — HTTP contract en `test_universidades_router.py` + DB real en `test_universidad_repository.py`
- [x] 2.7 Tests: ABM — superadmin lista/crea/edita/da de baja; ADMIN no superadmin recibe 403; nombre duplicado 409; id inexistente 404 — `test_universidades_router.py` + `test_universidad_service.py`
- [x] 2.8 Tests: la baja es lógica — la fila sobrevive, las materias y comisiones asociadas no se tocan, y la universidad desaparece de `/universidades/mias` — `test_universidad_repository.py` (DB real, sqlite in-memory)
- [x] 2.9 `pytest -q` sin regresiones respecto de 0.1. **Resultado**: 1639 passed (+29 sobre el baseline de 1610), mismo 1 failed pre-existente (`test_pendientes_moodle_returns_424_when_no_credentials`)

## 3. Contexto de universidad activa (frontend)

- [x] 3.1 `shared/context/TenantProvider.tsx`: estado derivado de la sesión, expone `universidadActivaId`, `rol`, `esSuperadmin` y `cambiarUniversidad(id | null)` (D1). Reactividad intra-tab vía `AUTH_SESSION_EVENT` nuevo (custom event que `auth-service.persistirSesion` dispara — `localStorage`/`storage` no reaccionan en la misma pestaña que escribe)
- [x] 3.2 Montar el provider en `src/app/providers.tsx`, por dentro del `QueryClientProvider` para poder acceder al `queryClient`. **Desviación de diseño**: `cambiarUniversidad` NO navega internamente (a diferencia de lo que sugiere la tarea 3.4) — `TenantProvider` se monta ENVOLVIENDO `RouterProvider` (fuera de su contexto), así que `useNavigate()` ahí adentro rompería. El selector (3.5), que sí vive dentro del árbol enrutado, hace `await cambiarUniversidad(id)` y navega él mismo
- [x] 3.3 `features/universidades/services` + hook de React Query para `GET /universidades/mias`. Aprovechado para dejar también los hooks del ABM (`useUniversidades`/`useCreateUniversidad`/`useUpdateUniversidad`/`useDeleteUniversidad`, tarea 4.2) listos de una
- [x] 3.4 Implementar `cambiarUniversidad`: `POST /auth/switch-universidad` → reemplazar sesión → **`queryClient.clear()`** → navegar a `/dashboard` (D4). Ante error, conservar la sesión anterior y NO limpiar la caché. Navegación implementada en el `UniversidadSelector` (ver desviación anotada en 3.2), no dentro del provider
- [x] 3.5 Componente selector en el layout: universidades propias, marca de la activa, y opción "Todas las universidades" sólo para superadmin. Sin alternativas cuando hay una sola membresía. `UniversidadSelector.tsx`, montado en `Sidebar` (desktop) y en el bottom-sheet "Más" de `BottomNav` (mobile)
- [x] 3.6 Migrar las lecturas de `.rol` a `useTenant()` en los 16 archivos que salgan del inventario de 1.2. **Resultado real**: de los 16 archivos del grep original, sólo 8 leían el rol de la SESIÓN actual (`useAuth().user.rol`) — los otros 8 eran falsos positivos del grep (`.rol` de un `Usuario` administrado en un formulario/lista, o de un `filtro.rol`, sin relación con el tenant activo). Migrados: `Sidebar.tsx`, `BottomNav.tsx`, `RubricasPage.tsx`, `MateriasPage.tsx`, `EntregasPage.tsx`, `DashboardPage.tsx`, `ComisionesPage.tsx`, `ComisionForm.tsx`. `PerfilPage.tsx` NO se tocó a propósito: `PerfilResponse.rol` sigue siendo `usuarios.rol` global por diseño del backend (Non-Goal explícito del change, Fase 6). **Nota de refactor**: `useTenant` se separó de `TenantProvider.tsx` a un archivo propio (`useTenant.ts` + `tenant-context.ts`) porque `react-refresh/only-export-components` no permite mezclar un componente con otros exports en el mismo archivo — comportamiento verificado contra `npm run lint`, no una suposición
- [x] 3.7 Tests: `useTenant` devuelve el rol de la membresía activa y cambia al switchear, sin recargar — `src/shared/context/TenantProvider.test.tsx`
- [x] 3.8 Tests: el switch llama a `queryClient.clear()`; un switch fallido NO lo llama y deja la sesión intacta — `TenantProvider.test.tsx`
- [x] 3.9 Test del escenario de filtración: datos cargados bajo la universidad A no quedan disponibles tras cambiar a B — `TenantProvider.test.tsx` (siembra la caché, verifica que `clear()` la borra — no `invalidateQueries`, que serviría el dato viejo mientras revalida)
- [x] 3.10 `npm run typecheck`, `npm run lint` y `npx vitest run` limpios. **Resultado**: typecheck 0 errores; lint 70 problemas (idéntico al baseline real, verificado con `git stash -u` para excluir archivos nuevos y confirmar 0 regresiones); vitest 24 archivos / 123 tests, todos pasando

## 4. ABM de Universidades (frontend)

- [x] 4.1 `features/universidades/types` y `services`: listado, alta, edición y baja contra los endpoints de la Parte 2. Implementado junto con 3.3 (misma feature, un solo service)
- [x] 4.2 Hooks de React Query para el ABM, con invalidación del listado tras cada mutación. Implementado junto con 3.3 (`useCreateUniversidad`/`useUpdateUniversidad`/`useDeleteUniversidad`)
- [x] 4.3 `UniversidadesPage`: tabla con nombre, campus, estado y acciones; toggle de inactivas
- [x] 4.4 Formulario de alta/edición (`nombre`, `moodle_host`, `activa`) con React Hook Form + Zod, siguiendo el patrón de las demás pantallas
- [x] 4.5 Ruta lazy `/universidades` y entrada de navegación visible sólo con `esSuperadmin`. Entrada de nav con `roles: []` (el mecanismo existente de `puedeVer` ya cubre "sólo superadmin" sin cambios); ruta protegida con `SuperadminRoute` nuevo (redirige a `/dashboard` si no es superadmin — la URL es alcanzable a mano aunque el menú esté oculto)
- [x] 4.6 Tests: un no-superadmin no ve la entrada de menú ni puede entrar a la ruta — `navConfig.test.ts` (menú) + `SuperadminRoute.test.tsx` (ruta)
- [x] 4.7 Tests de la página: listado, alta, edición y baja con el servicio mockeado — `UniversidadesPage.test.tsx`

## 5. Cierre

- [x] 5.1 Suite completa: `pytest -q` en backend y `npm run typecheck && npm run lint && npx vitest run` en frontend, comparando contra el baseline de 0.1. **Resultado**: backend 1639 passed (+29 vs. baseline 1610), mismo 1 failed pre-existente; frontend typecheck 0 errores, lint 70 problemas (idéntico al baseline), vitest 26 archivos / 131 tests (baseline 15/73) todos pasando
- [x] 5.2 Verificación manual contra el backend levantado de los tres perfiles: una universidad, dos universidades, superadmin. **NO VERIFICADO**: se confirmó que `passlib`/`bcrypt` sigue roto en este entorno (`AttributeError: module 'bcrypt' has no attribute '__about__'`, reproducido corriendo el test que ya lo evidenciaba) — cualquier login real dispara `verify_password`/hashing, que rompe con este error. No se levantó el stack completo ni se simuló una verificación; queda pendiente para un entorno con `passlib`/`bcrypt` compatibles
- [x] 5.3 Documentar en el resumen las Open Questions del design que sigan abiertas (membresías desde la UI, escritura en modo global) y recordar OP-1 y OP-2 como precondiciones de despliegue. Ver resumen final de la sesión
